// src/pages/Categorias.jsx
import Productos from "./Productos.jsx";

// Wrapper simple que reusa la vista de Productos.
// Si más adelante quieres slug por categoría, lo ampliamos.
export default function Categorias() {
  return <Productos />;
}
